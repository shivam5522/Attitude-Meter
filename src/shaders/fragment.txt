#version 330 core

in vec3 fragmentColor;

out vec4 screenColor;

uniform vec4 uColor;

void main(){
    screenColor = vec4(fragmentColor, 1.0);
    if(uColor.a >0.0){
        screenColor = uColor;
    }
}